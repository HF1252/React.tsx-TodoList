import React from 'react';
import styles from './styles';

export const Header = () => {
    const { Wrapper, TopLabel } = styles;
    return (
        <Wrapper>
            <TopLabel></TopLabel>
        </Wrapper>
    );
};
